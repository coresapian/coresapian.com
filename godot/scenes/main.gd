extends Node

## Coresapian — stripped-down single player. No multiplayer, no chat.
## Just load the temple and spawn a player.

func _ready() -> void:
	print("[Main] _ready — single player mode, no MP")
	# Nothing else to do. temple.tscn handles everything.
