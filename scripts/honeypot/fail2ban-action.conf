# Fail2Ban action — trigger honelert.sh on ban
# Install to: /etc/fail2ban/action.d/coresapian-honeypot-alert.conf

[Definition]
actionstart =
actionstop =
actioncheck =
actionban = /usr/local/bin/honelert.sh "<ip> - - [<time>] \"GET <path> HTTP/1.1\" 200 0" 2>/dev/null || true
actionunban =
